node_modules/
src-tauri/target/
src-tauri/gen/schemas/
dist/
scripts/paste_target
.DS_Store
*.log
.env
.env.*
*.key
secrets*
# Credential-store implementation is source code and must be included in clean clones.
!src-tauri/src/secrets.rs

# Ignore local Supabase CLI state, but keep migrations and function source.
supabase/.temp/
supabase/.branches/

# Private signing material and local verification artifacts
*.p8
*.p12
*.pfx
*.mobileprovision
# Keep example configuration without real credentials.
!.env.example
