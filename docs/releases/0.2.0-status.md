# Version 0.2.0 release status

The release remains a draft until production email delivery is configured and verified. The code and backend are implemented; this is an external mail setup requirement, not a completed public launch.

## Verified

- macOS: 35 Rust tests, strict Clippy, a real Keychain read/write/delete round trip, and a clean tracked-source build passed.
- Backend: Deno checks/tests, transactional SQL quota tests, and live free-account authentication, transcription, usage accounting, quota rejection, and refresh passed. Test accounts were removed. The Auth test redeemed an admin-generated code without sending mail.
- macOS app and DMG: Developer ID signed, accepted by Apple, stapled, and accepted by Gatekeeper. Downloads from the GitHub draft matched local SHA-256 hashes; the contained app was version 0.2.0 and notarized. The updater archive retained the notarization ticket after extraction.
- Native Windows x64: [GitHub Actions run 33990373020](https://github.com/sarrazola/dictamelo/actions/runs/33990373020) passed 36 Rust tests and built the NSIS installer. The application payload's PE machine type was verified as `0x8664`. Its source commit was `4b06ee9`; application source/manifests matched the final macOS source.
- The native x64 installer was signed on the Mac with the existing Tauri updater key and uploaded to the draft. No private key was given to GitHub Actions.
- Windows ARM64: built, installed, and upgraded from 0.1.2 with settings, history, and credentials preserved. Transcription, pasting, and clipboard restoration passed. Both Windows architectures installed with the correct payload architecture and version; x64 ran under ARM emulation. See the [Windows report](../WINDOWS_BUILD_REPORT.md) for exact coverage and limitations.
- Windows release helpers: both PowerShell scripts parsed cleanly; a dry run staged both targets without changing the draft. Attempting to upload to public v0.1.2 was rejected before any build or asset change.
- Complete release: all nine assets were downloaded afresh from the GitHub draft. Every SHA-256 checksum and all three Tauri updater signatures passed verification against the app's embedded public key. `latest.json` contains `darwin-aarch64`, `windows-aarch64`, and `windows-x86_64`.

## Remaining before publication

1. Configure a production SMTP sender for Supabase project `iburiyhhfodndqgmsaot`. The project currently has no SMTP host/sender. The default mail service does not support arbitrary public users. Both required code email templates and confirmed-email authentication are already configured.
2. Verify actual email delivery and code redemption, then remove the pending status notice from README, update this record, and publish using `scripts/release.sh 0.2.0`.

After publication, verify the public downloads and an actual in-app update from 0.1.2. The successful Windows installer upgrade and valid signatures do not substitute for that check.

Existing public version 0.1.2 remains available while this draft is completed. Its historical installers were not notarized. The new notarized 0.2.0 Mac artifacts are in the authenticated GitHub draft and the ignored local `dist/v0.2.0/` staging folder.
